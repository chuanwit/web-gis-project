# 智慧城市-武汉 大改方案 v1.0

> 基于 6 份参考作品提炼的优点与创新点，对 smart-city 进行系统性升级。
> 参考4（华农三引擎+AI）、参考5（丝路 Pinia 中枢+三级降级）、参考6（热环境业务闭环）为核心借鉴来源。

---

## 一、参考作品优点提炼（已落地依据）

| 来源 | 借鉴点 | 落地到本项目 |
|------|--------|-------------|
| 参考4 | DeepSeek+LangChain+流式SSE+RAG | AI 智能助手升级为真实大模型对话 |
| 参考4 | 飞线辐射动画、图表矩阵+地图联动高亮 | 可视化增强 |
| 参考4 | Loading 动画、实时天气、一键回首页 | 体验优化 |
| 参考5 | Pinia 单一状态中枢 + watch 驱动联动 | 架构升级（替代模块级单例） |
| 参考5 | 三级优雅降级 | 数据降级容错策略 |
| 参考6 | 业务闭环：态势感知→风险诊断→资源可达→情景推演→成果输出 | 业务模块化 |
| 参考6 | 降温情景推演（选片区→勾选策略→预估→运行模拟） | 交通情景推演 |
| 参考6 | 登录界面、四区布局、指标卡片矩阵 | 体验+布局 |

## 二、现状诊断

- **架构**：provide/inject + 模块级 reactive 单例（useTimeOfDay/useLayerToggles），扩展性差，无 Pinia/Router
- **业务**：仅"展示+工具"（事故查询/测量/路径），无决策闭环
- **AI**：mock 端规则生成，非真实大模型
- **数据**：13430 建筑 / 2314 道路 / 50 事件（5 类×5 区域），无容错降级
- **可视化**：无飞线、无图表联动、无 KPI 卡片矩阵、无实时天气

## 三、分阶段实施计划

### 阶段一：架构升级（基础设施）

**目标**：引入 Pinia 状态中枢 + 数据降级 + Loading 体验，为后续模块打地基。

1. 安装依赖：`pinia`
2. 新建 `src/stores/` 目录：
   - `map.js`：scene/map 实例（替代 provide/inject 的运行时注入，保留 inject 兼容）
   - `time.js`：hour/period/playing（迁移 useTimeOfDay）
   - `layers.js`：heatmap/scatter/model3d 开关（迁移 useLayerToggles）
   - `business.js`：当前业务模块（overview/risk/resource/simulation）+ 选中的区域/事故
   - `ai.js`：对话历史 + 流式状态
   - `data.js`：建筑/道路/事件数据缓存 + 降级标记
3. `main.js` 注册 pinia
4. 新建 `src/utils/dataFallback.js`：统一的"请求→缓存→降级"工具，单源失败不致命
5. 新建 `src/components/Loading.vue`：品牌字母渐出 + 进度条，地图就绪后淡出
6. 全局 CSS 变量主题：`src/assets/styles/theme.css`（科技蓝青配色体系）

**验收**：现有功能不回归 + Loading 出现 + 控制台无报错。

### 阶段二：业务闭环化（核心加分项）

**目标**：顶部业务导航切换四大模块，从"展示"升级为"决策平台"。

顶部新增 `BusinessNav.vue`（4 个 Tab）：

| 模块 | 地图焦点 | 左侧面板 | 右侧面板 | KPI 卡片(顶部) |
|------|---------|---------|---------|---------------|
| 综合态势 | 城市俯瞰 | 出行人口/公交（现有） | 人口/医院/高校（现有） | 事件总数/道路均拥堵/覆盖区域/当前时段 |
| 交通风险诊断 | 流芳校区事件热区 | 风险贡献雷达图 + 事件类型分布 | 区域风险排行榜（5 区域） + 高风险事件列表 | 高风险事件数/最堵路段/风险等级/影响人口 |
| 应急资源可达 | 资源点+服务半径 | 资源分类筛选 + 覆盖率 | 片区资源覆盖效能 + 资源缺口 | 资源总数/覆盖率/平均响应/缺口区域 |
| 情景推演优化 | 选定片区 | 策略勾选（信号优化/分流/救护调度/限行） | 预估指标变化 + 运行模拟 | 当前风险/预计降幅/覆盖提升/恢复时长 |

**关键交互**：
- Tab 切换 → `business.js` 模块变更 → watch 驱动地图 flyTo + 面板内容切换 + KPI 重算
- 情景推演：勾选策略实时预估 → "运行模拟"按钮 → 地图高亮 + 指标卡片更新
- 区域风险排行榜：点击区域 → 地图 flyTo + 高亮该区域事件

**新增数据**：mock 增加区域多边形（5 区域）、资源点（医院/消防/交警，复用事件点坐标衍生）、区域统计指标。

### 阶段三：AI 真实大模型

**目标**：接入 DeepSeek，流式对话 + 自然语言查询 + 数据溯源。

1. 新建 `server/` 目录：Node Express 代理服务
   - `POST /api/ai/chat`（SSE 流式）：转发 DeepSeek `chat/completions`（stream:true）
   - 从 `.env` 读取 `DEEPSEEK_API_KEY`；缺失则走"增强规则+流式模拟"兜底
   - 注入数据上下文（当前时段/事件统计/区域风险）作为 system prompt
2. 前端新建 `src/components/AiAssistant/`：
   - `AiAssistant.vue`：右下角悬浮入口 + 展开抽屉式对话窗
   - 流式打字机渲染（逐字）
   - 快捷问题芯片（"当前哪个区域风险最高""对比区域1和区域3""推荐处置策略"）
   - 数据溯源标签（引用了哪些数据）
3. 保留原 AiAnalysisCard 作为"事故一键分析"快捷入口，复用同一 AI store
4. `vite.config.js` 配置 dev proxy 转发 `/api/ai/chat` 到 Express（端口 3001）

**降级**：API Key 缺失/请求失败 → 本地规则引擎生成 + 模拟流式输出，答辩不中断。

### 阶段四：可视化增强

1. **飞线辐射**：`useFlyline.js` hook，武汉中心→5 区域弧线飞行动画（L7 LineLayer+icon）
2. **图表矩阵联动**：点击地图事件 → 所有相关图表高亮该区域/类型；点击图表 → 地图 flyTo+高亮
3. **拉框动态聚合**：DrawTools 拉框后，左侧面板实时重算框内统计（事件数/类型分布/拥堵均值）
4. **实时天气**：顶部 Header 显示武汉天气（和风天气 API 或 mock 兜底，随时间轴时段变化）
5. **3D 挤出建模**：5 区域多边形按风险值挤出高度（L7 PolygonLayer extrude）

### 阶段五：体验优化

1. **登录界面**：`Login.vue` 全屏，角色选择（指挥员/分析员/管理员）+ 账号密码（任意非空即可），过渡动画进入主屏
2. **一键回首页**：Header Logo 点击 → 回到综合态势 + 城市俯瞰视角
3. **全局科技感主题**：CSS 变量统一（青蓝主色+霓虹强调）、卡片毛玻璃+扫边光、按钮微交互
4. **Loading 品牌化**：阶段一的 Loading 配合主题细化

## 四、技术决策

- **保留 L7+Mapbox**：不引入 Three.js/Cesium（避免依赖膨胀，现有引擎足够）
- **Pinia 替代模块单例**：更规范，支持 devtools 调试，为业务模块状态铺路
- **Express 独立后端**：DeepSeek 需服务端代理（隐藏 Key + CORS），与 vite-plugin-mock 共存（mock 处理数据接口，Express 处理 AI 流式）
- **渐进式迁移**：composables 保留薄封装层代理到 Pinia，老组件不改也能跑，分批迁移

## 五、风险与兜底

| 风险 | 兜底方案 |
|------|---------|
| DeepSeek API Key 缺失/限流 | 增强规则引擎 + 模拟流式，体验一致 |
| Mapbox Token 失效 | 现有 token 仍可用；增加加载失败提示 |
| mock 数据接口异常 | dataFallback 工具：缓存命中 + 降级标记 + UI 提示 |
| 改动导致回归 | 每阶段验收点：现有功能不回归才进入下一阶段 |

## 六、验收清单

- [ ] 阶段一：Loading 出现、Pinia 接管状态、现有图层/工具/时间轴正常
- [ ] 阶段二：4 模块 Tab 切换、情景推演可运行、KPI 卡片正确
- [ ] 阶段三：AI 对话流式输出、自然语言查询有响应、降级可用
- [ ] 阶段四：飞线可见、图表联动、拉框聚合、天气显示、3D 区域挤出
- [ ] 阶段五：登录进入、一键回首页、主题统一

